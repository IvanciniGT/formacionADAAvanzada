project Fichero is

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;

   for Source_Dirs use (".");
   for Exec_Dir use "bin";
   for Object_Dir use "obj";
   for Main use ("main.adb");

end Fichero;
